// Botica Natural - Home JS
document.addEventListener('DOMContentLoaded', () => {
    console.log('Botica Natural Home Loaded');
    
    // Example: Smooth scroll for hero buttons
    const heroButtons = document.querySelectorAll('.hero .btn-primary, .hero .btn-outline');
    heroButtons.forEach(btn => {
        btn.addEventListener('click', (e) => {
            // Add custom behavior if needed
        });
    });
});
